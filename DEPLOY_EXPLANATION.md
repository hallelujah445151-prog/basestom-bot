# Как работает деплой проекта на сервере

## ЛОГИКА РАЗВЕРТЫВАНИЯ

### Что происходит при деплое:

**1. Создание архива (ZIP)**
   ```
   Включенные файлы:
   - requirements.txt
   - DEPLOY_COMMANDS.md
   - supervisor.conf
   - src/ (весь код)
   - data/references.json
   - src/.env (токены)

   Исключенные файлы (НЕ включены в архив):
   - .git/
   - __pycache__/
   - *.pyc
   - venv/ (виртуальное окружение)
   - .env (в корне проекта)
   - data/orders.db (БАЗА ДАННЫХ!)
   - *.log
   - test_*.py
   ```

**2. Загрузка архива на сервер**
   ```bash
   scp basestom-bot-deploy.zip root@31.129.99.125:/tmp/
   ```

**3. Распаковка на сервере**
   ```bash
   cd /opt/basestom-bot
   unzip -o /tmp/basestom-bot-deploy.zip -d /opt/basestom-bot
   ```

**Ключевой флаг: -o** означает "overwrite files without prompting"
   - ✅ Файлы ПЕРЕЗАПИСЫВАЮТСЯ (не дублируются)
   - ✅ Старые файлы заменяются на новые
   - ❌ Конфликты НЕ происходят

**4. Установка зависимостей**
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt
   ```

**5. Перезапуск бота**
   ```bash
   supervisorctl restart basestom-bot
   supervisorctl status basestom-bot
   ```

## ОТВЕТ НА ВОПРОСЫ

### ❓ Файлы заменяются или дублируются?

**Ответ:** Файлы ЗАМЕНЯЮТСЯ (перезаписываются), не дублируются.

**Почему:**
- Используется флаг `-o` в команде `unzip`
- Это означает "overwrite files without prompting"
- Старые файлы заменяются на новые версии

### ❓ Могут ли быть конфликты?

**Ответ:** НЕТ, конфликты НЕ происходят.

**Почему:**

**1. База данных:**
   - ❌ Файл `data/orders.db` НЕ включен в архив
   - ✅ База данных остается НЕТРОНУТОЙ на сервере
   - ✅ Заказы, напоминания, пользователи - все сохранено
   - ✅ Конфликты невозможны

**2. Виртуальное окружение:**
   - ❌ Папка `venv/` НЕ включена в архив
   - ✅ Старые зависимости остаются
   - ✅ Конфликты невозможны

**3. Код приложения:**
   - ✅ Перезаписывается (это ПРАВИЛЬНО)
   - ✅ Старый код заменяется на новый
   - ✅ Новые исправления применяются

**4. Токены и конфигурация:**
   - ⚠️ Файл `src/.env` ПЕРЕЗАПИСЫВАЕТСЯ
   - ✅ Это ПРАВИЛЬНО (вместе с кодом деплоим актуальные токены)
   - ✅ Конфликты невозможны

## ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема 1: Потеря локальных изменений на сервере

**Сценарий:** Если вы изменили какой-то файл на сервере напрямую (без Git), то он будет потерян при деплое.

**Решение:** Используйте Git для управления версиями.

**Процесс:**
1. Внесите изменения локально
2. Закоммить изменения: `git add . && git commit -m "Описание"`
3. Загрузите на Git: `git push`
4. На сервере: `cd /opt/basestom-bot && git pull`

### Проблема 2: Пауза бота во время деплоя

**Сценарий:** Бот останавливается на ~30 секунд во время перезапуска.

**Решение:** Это нормально и ожидаемо.

### Проблема 3: Ошибка зависимости

**Сценарий:** Если в `requirements.txt` добавилась новая библиотека, она будет установлена.

**Решение:** Автоматически устанавливается при деплое.

## БЕЗОПАСНОСТЬ

### ✅ Что безопасно:

**База данных:**
- ❌ НЕ включается в архив
- ✅ Остается нетронутой
- ✅ Заказы, пользователи, напоминания - все сохранено
- ✅ Конфликты невозможны

**Виртуальное окружение:**
- ❌ НЕ включается в архив
- ✅ Старые зависимости остаются
- ✅ Конфликты невозможны

### ✅ Что обновляется (ПРАВИЛЬНО):

**Код приложения:**
- ✅ Перезаписывается
- ✅ Новые исправления применяются
- ✅ Баги исправляются

**Токены:**
- ✅ Перезаписываются (вместе с кодом)
- ✅ Актуальные ключи применяются

## ЛУЧШАЯ ПРАКТИКА

### Рекомендация 1: Используйте Git

```bash
# Локально
git add .
git commit -m "Исправление напоминаний"
git push

# На сервере
cd /opt/basestom-bot
git pull
supervisorctl restart basestom-bot
```

### Рекомендация 2: Резервное копирование базы данных

```bash
# Перед деплоем
ssh root@31.129.99.125 "cd /opt/basestom-bot && cp data/orders.db data/orders.db.backup.$(date +%Y%m%d_%H%M%S)"
```

### Рекомендация 3: Тестируйте на локальной базе

```bash
# Скопируйте базу с сервера перед тестами
scp root@31.129.99.125:/opt/basestom-bot/data/orders.db .
```

## ИТОГ

✅ **Файлы ПЕРЕЗАПИСЫВАЮТСЯ (не дублируются)**

✅ **КОНФЛИКТЫ НЕ ПРОИСХОДЯТ**

✅ **БАЗА ДАННЫХ НЕ ИСПЫТЫВАЕТ**

✅ **Это ПРАВИЛЬНЫЙ и БЕЗОПАСНЫЙ метод деплоя**
