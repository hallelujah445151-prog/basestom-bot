# Как запустить бота на Replit

## Инструкция для деплоя:

### 1. Создание репозитория на GitHub (если еще нет)

1. Откройте https://github.com/
2. Зарегистрируйтесь или войдите
3. Нажмите "+" → "New repository"
4. Назовите: `basestom-bot`
5. Описание: `Telegram бот для управления заказами в зуботехнической лаборатории`
6. Выберите "Public" или "Private"
7. Нажмите "Create repository"

### 2. Загрузка проекта на GitHub

**Вариант А: Через Git (рекомендую)**
```bash
cd C:\Users\crush\AppData\Roaming\projects\basestom
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/hallelujah44/basestom-bot.git
git push -u origin main
```

**Вариант Б: Через интерфейс GitHub**
1. На странице репозитория нажмите "Upload files"
2. Загрузите ВСЕ файлы из папки `basestom`
3. Введите описание: "Initial commit"
4. Нажмите "Commit changes"

### 3. Создание проекта на Replit

1. Откройте https://replit.com/
2. Нажмите "+" → "Create Repl"
3. Выберите "Import from GitHub"
4. Введите: `hallelujah44/basestom-bot`
5. Или вставьте GitHub URL репозитория
6. Нажмите "Create Repl"

### 4. Настройка переменных окружения на Replit

1. В Replit нажмите на иконку "Secrets" (иконка замка 🔒) в панели слева
2. Нажмите "New Secret"
3. Добавьте переменные:
   - Key: `BOT_TOKEN`
     - Value: `8592737363:AAGK2R2KxJuGY9-RPZlBq2YBupKz0NAr0H8`
   - Key: `OPENROUTER_API_KEY`
     - Value: `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`
4. Нажмите "Add"

### 5. Запуск бота

1. В Replit нажмите кнопку "Run" (зеленая кнопка ▶️)
2. В консоли должно появиться:
   ```
   Bot started...
   Reminder background task started...
   Reminder background task started (checking every 5 minutes)
   ```
3. Бот начнет работать автоматически!

### 6. Проверка работоспособности

1. Откройте Telegram
2. Найдите бота: @sfdtgafvdba_bot
3. Напишите `/start`
4. Должно прийти приветственное сообщение

## Файлы, которые нужны для деплоя:

✅ **Обязательно:**
- `.replit` - конфигурация Replit
- `src/` - весь код бота
- `data/references.json` - реестры
- `requirements.txt` - зависимости Python
- `.env` - переменные окружения (с токенами)
- `README.md` - документация

❌ **Не обязательно:**
- `test_bot.py` - тестовые файлы
- `test_send.py` - тестовые файлы
- `AGENTS.md` - только для ИИ агента
- `references.md` - требования к проекту

## Преимущества Replit:

✅ **Полностью бесплатно**
✅ **"Always On"** - работает постоянно, не спит
✅ **Автоматический запуск** при старте
✅ **Логи в реальном времени**
✅ **Простая настройка**
✅ **Встроенный терминал**
✅ **Обновления через git push**

## Возможные проблемы:

❌ **Бот не отвечает:**
- Проверьте токены в Secrets
- Проверьте, что bot.py запущен (консоль)

❌ **Ошибка импорта:**
- Проверьте структуру файлов
- Все файлы должны быть на своих местах

❌ **База данных:**
- Создастся автоматически при первом запуске
- Не теряйте `data/orders.db`!

## Резервное копирование:

⚠️ **ВАЖНО:** База данных (orders.db) не сохраняется на Replit при каждом запуске!

**Решение:** Используйте внешнюю БД или сохраняйте копии:
1. Регулярно скачивайте `data/orders.db` через консоль
2. Или перейдите на платный VPS с постоянным хранилищем

## Дополнительно:

- Для постоянной работы оставьте вкладку Replit открытой
- Бот будет работать пока Replit активен
- При неактивности Replit может перейти в спящий режим
